import { {{pascalCase name}}Props } from './{{pascalCase name}}.types';
import { Outlet } from 'react-router'; // or 'react-router-dom' depending on setup

export function {{pascalCase name}}({ children }: {{pascalCase name}}Props) {
  return (
    <div className="flex min-h-screen flex-col">
      <header className="h-16 border-b">
        {/* Header content */}
      </header>
      
      <main className="flex-1">
        {children || <Outlet />}
      </main>
      
      <footer className="h-16 border-t">
        {/* Footer content */}
      </footer>
    </div>
  );
}
