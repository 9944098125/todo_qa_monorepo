import { RootState } from '@/store';

export const select{{pascalCase name}}Data = (state: RootState) => state.{{camelCase name}}.data;
export const select{{pascalCase name}}IsLoading = (state: RootState) => state.{{camelCase name}}.isLoading;
export const select{{pascalCase name}}Error = (state: RootState) => state.{{camelCase name}}.error;
