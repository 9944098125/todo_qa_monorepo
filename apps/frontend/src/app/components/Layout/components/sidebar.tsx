import React from 'react';
import { Button } from '../../ui/button';
import { Sun } from 'lucide-react';

export function Sidebar() {
  return (
    <React.Fragment>
      <div
        id="sidebar"
        className="h-full w-full flex justify-between items-center px-10 bottom right-0"
      ></div>
    </React.Fragment>
  );
}
